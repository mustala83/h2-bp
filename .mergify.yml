queue_rules:
  - name: default
    queue_conditions:
      - base=main
    merge_conditions:
      - check-success=required-ci

pull_request_rules:
  - name: h2 queue
    conditions:
      - base=main
    actions:
      queue:
        name: default
